// Run: npm init -y
// Then install dependencies: npm install express mongoose cors dotenv
// Optional (for dev): npm install --save-dev nodemon

"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js"
}
